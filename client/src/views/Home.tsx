import { FC } from 'react';
import NavTemplate from '@/components/templates/Nav-template';

const Home: FC = () => {
    return (
        <NavTemplate>
          <p>Hello main</p>
        </NavTemplate>
    );
};

export default Home;
